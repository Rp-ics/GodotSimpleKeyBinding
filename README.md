# GodotSimpleKeyBinding
Godot 4 rebinding system with multi-slot bindings (keyboard, mouse, joypad). Supports 4 inputs per action, auto-conflict resolution, persistent save/load, and reset to defaults. Full UI generation with customizable colors/fonts via exports.
